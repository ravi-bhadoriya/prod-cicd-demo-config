apiVersion: apps/v1
kind: Deployment

metadata:
  name: prod-cicd-demo
  namespace: prod-demo

spec:
  replicas: 2

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  selector:
    matchLabels:
      app: prod-cicd-demo

  template:
    metadata:
      labels:
        app: prod-cicd-demo

    spec:
      containers:
      - name: app
        image: 779599553744.dkr.ecr.ap-south-1.amazonaws.com/prod-cicd-demo:8e73144fece459fb8e3f6fb1ca698c8fe0f313a0
        imagePullPolicy: Always

        env:
        - name: APP_VERSION
          value: "8e73144"

        envFrom:
        - configMapRef:
            name: app-config

        ports:
        - containerPort: 5000

        readinessProbe:
          httpGet:
            path: /ready
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 20

        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"